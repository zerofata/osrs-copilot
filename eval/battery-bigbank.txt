# Bank-mode contract, summarized side. Run with:
#   gradlew evalRun -PevalSnapshot=eval\snapshot-sample-bigbank.json -PevalBattery=eval\battery-bigbank.txt -PevalRouteOnly=true
# That fixture's bank exceeds Router.BANK_INLINE_LIMIT, so routing flips to
# bank=summarized. Under this mode prefetch must attach the strategy page's
# equipment section (tables the plaintext extract strips) and an ownership
# slice of the items the facts mention; residual ownership checks the model
# makes must go through ONE batched search_owned_items call. Route-only pins
# the mode; full runs check the prefetch and the batching.

what should i bring for tormented demons
ASSERT {"monsters": ["Tormented Demon"], "needs": ["strategy"], "bank": ["summarized"]}
