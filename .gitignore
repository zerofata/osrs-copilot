.gradle
build
.idea/
.project
.settings/
.classpath
nbactions.xml
nb-configuration.xml
nbproject/
# Local development aids -- not part of the published plugin.
# eval/ holds recorded game snapshots and prompt dumps (personal game state):
# only the battery question files and the synthetic sample snapshot are
# shareable; everything else stays local.
eval/*
!eval/battery*.txt
!eval/snapshot-sample.json
!eval/make-sample-snapshot.py
run.ps1
AGENTS.md
CLAUDE.md